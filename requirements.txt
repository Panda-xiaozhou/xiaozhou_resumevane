# ============================================================
# ResumeVane Python 依赖
# ============================================================
# 安装: pip install -r requirements.txt
# 更新依赖版本后请测试全部 Agent 节点是否正常运行
# ============================================================

# ── Web 框架 ──────────────────────────────────────────
# FastAPI: 高性能异步 Web 框架
# uvicorn: ASGI 服务器（开发: --reload 热重载 / 生产: --workers N）
# python-multipart: 处理文件上传（候选人简历投递）
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19

# ── 数据库 ────────────────────────────────────────────
# SQLAlchemy 2.0: Python ORM
# psycopg2-binary: PostgreSQL 驱动（binary 版本无需编译）
# alembic: 数据库迁移工具（生产环境用，替代 create_all）
# pgvector: PostgreSQL 向量扩展的 Python 客户端
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.1
pgvector==0.3.6

# ── Agent 框架 ────────────────────────────────────────
# LangGraph: Agent 状态图编排（核心依赖）
# LangChain: 工具链基础库
# langchain-community: 社区工具集成
langgraph==0.2.60
langchain==0.3.13
langchain-community==0.3.13

# ── AI / LLM ──────────────────────────────────────────
# openai: 通过 OpenAI 兼容接口调用 DeepSeek API
# chromadb: 向量数据库（备选，当前以 pgvector 为主）
# tenacity: LLM 调用重试（指数退避）
openai==1.58.1
chromadb==0.5.23
tenacity==9.0.0

# ── 飞书 SDK ──────────────────────────────────────────
# lark-oapi: 飞书开放平台 Python SDK
# 当前未使用 SDK 全部功能，仅通过 httpx 直调 API（见 feishu_service.py）
# 保留此依赖用于未来迁移到官方 SDK
lark-oapi==1.4.11

# ── 文件处理 ──────────────────────────────────────────
# PyPDF2: PDF 文本提取
# python-docx: Word (.docx) 文本提取
# markdown: Markdown 转 HTML（报告渲染）
pypdf2==3.0.1
python-docx==1.1.2
markdown==3.7

# ── 认证 ──────────────────────────────────────────────
# python-jose: JWT Token 签发与验证
# passlib + bcrypt: 密码哈希
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# ── 任务队列 ──────────────────────────────────────────
# arq: 基于 Redis 的异步任务队列（比 Celery 更轻量、原生 async）
# redis: ARQ 依赖的 Redis 客户端
arq==0.26.0
redis==4.2.0

# ── 工具库 ────────────────────────────────────────────
# httpx: 异步 HTTP 客户端（飞书 API 调用）
# python-dotenv: 从 .env 文件加载环境变量
# aiofiles: 异步文件操作（简历上传）
# structlog: 结构化日志（JSON 格式，便于日志收集）
httpx==0.28.1
python-dotenv==1.0.1
aiofiles==24.1.0
structlog==24.4.0

# ── 测试 ──────────────────────────────────────────────
# pytest + pytest-asyncio: 异步测试支持
# pytest-cov: 覆盖率报告
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==6.0.0
